@page "/hotels/{HotelId:int}"
@page "/hotel-details"
@rendermode InteractiveServer
@using EventHotelBroker.Models
@using EventHotelBroker.Services
@using EventHotelBroker.Repositories
@using EventHotelBroker.Utils
@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims
@inject IHotelService HotelService
@inject IUnitOfWork UnitOfWork
@inject IBookingService BookingService
@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthStateProvider
@inject IJSRuntime JSRuntime

<PageTitle>@(hotel?.Name ?? "Hotel Details") - EventHotelBroker</PageTitle>

<link href="/css/hotel-details.css" rel="stylesheet" />

<div class="hotel-details-page">
    @if (isLoading)
    {
        <div class="loading-state">
            <div class="spinner-modern"></div>
            <p class="loading-text">Loading hotel details...</p>
        </div>
    }
    else if (hotel == null)
    {
        <div class="error-state">
            <i class="bi bi-exclamation-triangle error-icon"></i>
            <h2 class="error-title">Hotel Not Found</h2>
            <p class="error-text">The hotel you're looking for doesn't exist or has been removed.</p>
            <a href="/search" class="btn-modern btn-gradient-primary">
                <i class="bi bi-arrow-left me-2"></i>Back to Search
            </a>
        </div>
    }
    else
    {
        <!-- Breadcrumb -->
        <div class="breadcrumb-container">
            <nav class="breadcrumb-nav">
                <a href="/" class="breadcrumb-link"><i class="bi bi-house"></i> Home</a>
                <i class="bi bi-chevron-right breadcrumb-separator"></i>
                <a href="/search" class="breadcrumb-link">Search</a>
                <i class="bi bi-chevron-right breadcrumb-separator"></i>
                <span class="breadcrumb-current">@hotel.Name</span>
            </nav>
        </div>

        <!-- Hotel Header -->
        <div class="hotel-header">
            <div class="hotel-header-content">
                <h1 class="hotel-title">@hotel.Name</h1>
                <p class="hotel-location">
                    <i class="bi bi-geo-alt"></i> @hotel.Address, @hotel.City, @hotel.Country
                </p>
            </div>
            @if (hotel.IsApproved)
            {
                <div class="verified-badge">
                    <i class="bi bi-patch-check-fill"></i>
                    <span>Verified</span>
                </div>
            }
        </div>

        <div class="hotel-content">
            <!-- Main Content -->
            <div class="content-main">
                <!-- Image Gallery -->
                <div class="gallery-section">
                    @if (hotel.Images.Any())
                    {
                        <!-- Main Image Display -->
                        <div class="main-image-container">
                            <img src="@(selectedImage ?? hotel.Images.First().Url)" alt="@hotel.Name" class="main-image" />
                            <div class="image-counter">
                                <i class="bi bi-images"></i> @(selectedImageIndex + 1) / @hotel.Images.Count
                            </div>
                        </div>

                        <!-- Thumbnail Grid -->
                        <div class="thumbnail-grid">
                            @for (int i = 0; i < hotel.Images.Count; i++)
                            {
                                var index = i;
                                var image = hotel.Images.ElementAt(i);
                                <div class="thumbnail-item @(selectedImageIndex == index ? "active" : "")" 
                                     @onclick="() => SelectImage(index, image.Url)">
                                    <img src="@image.Url" alt="@hotel.Name - Image @(i + 1)" />
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="gallery-placeholder">
                            <i class="bi bi-building"></i>
                        </div>
                    }
                </div>

                <!-- Description -->
                <div class="info-section">
                    <h3 class="section-title">
                        <i class="bi bi-info-circle me-2"></i>About This Venue
                    </h3>
                    <p class="section-text">@hotel.Description</p>
                </div>

                <!-- Detail Tabs -->
                <div class="hd-tabs">
                    <button class="hd-tab @(activeDetailTab == "amenities" ? "active" : "")" @onclick='() => SetActiveTab("amenities")'>
                        <i class="bi bi-stars"></i> Amenities
                    </button>
                    <button class="hd-tab @(activeDetailTab == "specs" ? "active" : "")" @onclick='() => SetActiveTab("specs")'>
                        <i class="bi bi-list-check"></i> Specifications
                    </button>
                    <button class="hd-tab @(activeDetailTab == "policies" ? "active" : "")" @onclick='() => SetActiveTab("policies")'>
                        <i class="bi bi-file-text"></i> Policies
                    </button>
                    <button class="hd-tab @(activeDetailTab == "location" ? "active" : "")" @onclick='() => SetActiveTab("location")'>
                        <i class="bi bi-geo-alt"></i> Location
                    </button>
                    <button class="hd-tab @(activeDetailTab == "reviews" ? "active" : "")" @onclick='() => SetActiveTab("reviews")'>
                        <i class="bi bi-star"></i> Reviews
                    </button>
                </div>

                <div class="hd-tab-content">
                    @if (activeDetailTab == "amenities")
                    {
                        @if (hotel.HotelAmenities.Any())
                        {
                            <div class="amenities-grid">
                                @foreach (var hotelAmenity in hotel.HotelAmenities)
                                {
                                    <div class="amenity-item">
                                        <i class="bi bi-check-circle-fill"></i>
                                        <span>@hotelAmenity.Amenity?.Name</span>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="hd-tab-empty">
                                <i class="bi bi-stars"></i>
                                <p>No amenities listed yet</p>
                            </div>
                        }
                    }
                    else if (activeDetailTab == "specs")
                    {
                        <div class="specifications-grid">
                            <div class="spec-item">
                                <i class="bi bi-people-fill"></i>
                                <div class="spec-content">
                                    <span class="spec-label">Maximum Capacity</span>
                                    <span class="spec-value">@hotel.Capacity guests</span>
                                </div>
                            </div>
                            <div class="spec-item">
                                <i class="bi bi-currency-dollar"></i>
                                <div class="spec-content">
                                    <span class="spec-label">Price Per Night</span>
                                    <span class="spec-value">@hotel.Currency @hotel.PricePerNight.ToString("N0")</span>
                                </div>
                            </div>
                            <div class="spec-item">
                                <i class="bi bi-calendar-check"></i>
                                <div class="spec-content">
                                    <span class="spec-label">Listed Since</span>
                                    <span class="spec-value">@hotel.CreatedAt.ToString("MMMM yyyy")</span>
                                </div>
                            </div>
                            <div class="spec-item">
                                <i class="bi bi-shield-check"></i>
                                <div class="spec-content">
                                    <span class="spec-label">Verification Status</span>
                                    <span class="spec-value">@(hotel.IsApproved ? "Verified" : "Pending")</span>
                                </div>
                            </div>
                        </div>
                    }
                    else if (activeDetailTab == "policies")
                    {
                        <div class="policies-list">
                            <div class="policy-item">
                                <i class="bi bi-clock"></i>
                                <div class="policy-content">
                                    <strong>Check-in/Check-out:</strong>
                                    <span>Check-in from 2:00 PM, Check-out by 11:00 AM</span>
                                </div>
                            </div>
                            <div class="policy-item">
                                <i class="bi bi-x-circle"></i>
                                <div class="policy-content">
                                    <strong>Cancellation:</strong>
                                    <span>Free cancellation up to 48 hours before event date</span>
                                </div>
                            </div>
                            <div class="policy-item">
                                <i class="bi bi-credit-card"></i>
                                <div class="policy-content">
                                    <strong>Payment:</strong>
                                    <span>50% deposit required upon booking, balance due 7 days before event</span>
                                </div>
                            </div>
                            <div class="policy-item">
                                <i class="bi bi-exclamation-triangle"></i>
                                <div class="policy-content">
                                    <strong>House Rules:</strong>
                                    <span>No smoking indoors, respect noise curfew after 10:00 PM</span>
                                </div>
                            </div>
                        </div>
                    }
                    else if (activeDetailTab == "location")
                    {
                        <div class="location-container">
                            <div class="location-map-wrapper">
                                @if (hotel.Latitude.HasValue && hotel.Longitude.HasValue)
                                {
                                    <div id="hotelMap" style="height: 450px; width: 100%; border-radius: 16px; border: 1px solid rgba(158, 139, 99, 0.2); box-shadow: 0 4px 20px rgba(0,0,0,0.08);"></div>
                                }
                                else
                                {
                                    <div class="hd-tab-empty">
                                        <i class="bi bi-map"></i>
                                        <p>Map view not available for this property</p>
                                    </div>
                                }
                            </div>
                            
                            <div class="location-info-panel">
                                <h4 class="location-panel-title">Location Details</h4>
                                
                                <div class="location-info-grid">
                                    <div class="info-card">
                                        <i class="bi bi-geo-alt-fill"></i>
                                        <div class="info-content">
                                            <span class="info-label">Address</span>
                                            <span class="info-value">@hotel.Address</span>
                                        </div>
                                    </div>
                                    
                                    <div class="info-card">
                                        <i class="bi bi-building"></i>
                                        <div class="info-content">
                                            <span class="info-label">City</span>
                                            <span class="info-value">@hotel.City</span>
                                        </div>
                                    </div>
                                    
                                    <div class="info-card">
                                        <i class="bi bi-globe"></i>
                                        <div class="info-content">
                                            <span class="info-label">Country</span>
                                            <span class="info-value">@hotel.Country</span>
                                        </div>
                                    </div>
                                    
                                    @if (hotel.Latitude.HasValue && hotel.Longitude.HasValue)
                                    {
                                        <div class="info-card highlight">
                                            <i class="bi bi-pin-map-fill"></i>
                                            <div class="info-content">
                                                <span class="info-label">Coordinates</span>
                                                <span class="info-value">@hotel.Latitude.Value.ToString("F6"), @hotel.Longitude.Value.ToString("F6")</span>
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                    else if (activeDetailTab == "reviews")
                    {
                        <div class="reviews-summary">
                            <div class="rating-overview">
                                <div class="rating-score">4.5</div>
                                <div class="rating-stars">
                                    <i class="bi bi-star-fill"></i>
                                    <i class="bi bi-star-fill"></i>
                                    <i class="bi bi-star-fill"></i>
                                    <i class="bi bi-star-fill"></i>
                                    <i class="bi bi-star-half"></i>
                                </div>
                                <div class="rating-count">Based on 128 reviews</div>
                            </div>
                        </div>
                        <div class="reviews-list">
                            <div class="review-item">
                                <div class="review-header">
                                    <div class="reviewer-info">
                                        <div class="reviewer-avatar">JD</div>
                                        <div class="reviewer-details">
                                            <strong>Jane Doe</strong>
                                            <span class="review-date">2 weeks ago</span>
                                        </div>
                                    </div>
                                    <div class="review-rating">
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                    </div>
                                </div>
                                <p class="review-text">
                                    Excellent venue for our corporate event! The staff was professional and accommodating. 
                                    The facilities were clean and well-maintained. Highly recommended!
                                </p>
                            </div>
                            <div class="review-item">
                                <div class="review-header">
                                    <div class="reviewer-info">
                                        <div class="reviewer-avatar">MJ</div>
                                        <div class="reviewer-details">
                                            <strong>Michael Johnson</strong>
                                            <span class="review-date">1 month ago</span>
                                        </div>
                                    </div>
                                    <div class="review-rating">
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star-fill"></i>
                                        <i class="bi bi-star"></i>
                                    </div>
                                </div>
                                <p class="review-text">
                                    Great location and beautiful venue. Perfect for our wedding reception. 
                                    The only minor issue was parking, but overall a fantastic experience.
                                </p>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <!-- Booking Sidebar -->
            <div class="content-sidebar">
                <div class="booking-card">
                    <div class="booking-price">
                        <div class="price-amount">@hotel.Currency @hotel.PricePerNight.ToString("N0")</div>
                        <div class="price-period">per night</div>
                    </div>

                    <div class="booking-info">
                        <div class="info-item">
                            <i class="bi bi-people"></i>
                            <span>Up to @hotel.Capacity guests</span>
                        </div>
                        <div class="info-item">
                            <i class="bi bi-person"></i>
                            <span>@hotel.Owner?.FullName</span>
                        </div>
                    </div>

                    <div class="booking-divider"></div>

                    @if (!showBookingForm)
                    {
                        <div class="booking-actions">
                            <button type="button" class="btn btn-primary btn-lg w-100 mb-2" @onclick="HandleBookNow">
                                <i class="bi bi-credit-card me-2"></i>Book Now &amp; Pay
                            </button>
                            <button type="button" class="btn btn-outline-primary w-100" @onclick="SendMessage">
                                <i class="bi bi-envelope me-2"></i>Send Message
                            </button>
                        </div>
                    }
                    else
                    {
                        <div class="booking-form-container">
                            <h5 class="booking-form-title">Booking Request</h5>
                            
                            @if (!string.IsNullOrEmpty(bookingMessage))
                            {
                                <div class="alert-modern alert-@(bookingSuccess ? "success" : "danger")">
                                    <i class="bi bi-@(bookingSuccess ? "check-circle" : "exclamation-circle")"></i>
                                    @bookingMessage
                                </div>
                            }

                            <EditForm Model="bookingRequest" OnValidSubmit="SubmitBooking" FormName="bookingForm" class="booking-form">
                                <DataAnnotationsValidator />

                                <div class="form-group-modern">
                                    <label class="form-label-modern">
                                        <i class="bi bi-calendar-event"></i> Check-in Date
                                    </label>
                                    <InputDate @bind-Value="bookingRequest.StartDate" class="form-input-modern" />
                                    <ValidationMessage For="@(() => bookingRequest.StartDate)" class="validation-message" />
                                </div>

                                <div class="form-group-modern">
                                    <label class="form-label-modern">
                                        <i class="bi bi-calendar-check"></i> Check-out Date
                                    </label>
                                    <InputDate @bind-Value="bookingRequest.EndDate" class="form-input-modern" />
                                    <ValidationMessage For="@(() => bookingRequest.EndDate)" class="validation-message" />
                                </div>

                                <div class="form-group-modern">
                                    <label class="form-label-modern">
                                        <i class="bi bi-people"></i> Number of Guests
                                    </label>
                                    <InputNumber @bind-Value="bookingRequest.HeadCount" class="form-input-modern" />
                                    <ValidationMessage For="@(() => bookingRequest.HeadCount)" class="validation-message" />
                                </div>

                                <div class="form-group-modern">
                                    <label class="form-label-modern">
                                        <i class="bi bi-chat-text"></i> Additional Notes (Optional)
                                    </label>
                                    <InputTextArea @bind-Value="bookingRequest.Notes" class="form-input-modern" rows="3" />
                                </div>

                                <div class="booking-actions">
                                    <button type="submit" class="btn-modern btn-gradient-primary" disabled="@isSubmitting">
                                        @if (isSubmitting)
                                        {
                                            <span class="spinner-border spinner-border-sm me-2"></span>
                                        }
                                        else
                                        {
                                            <i class="bi bi-send me-2"></i>
                                        }
                                        Submit Request
                                    </button>
                                    <button type="button" class="btn-modern btn-outline-secondary" @onclick="() => showBookingForm = false">
                                        <i class="bi bi-x me-2"></i>Cancel
                                    </button>
                                </div>
                            </EditForm>
                        </div>
                    }
                </div>
            </div>
        </div>
    }
</div>

@code {
    [Parameter]
    public int HotelId { get; set; }

    [SupplyParameterFromForm]
    private BookingRequest bookingRequest { get; set; } = new();

    private Hotel? hotel;
    private bool isLoading = true;
    private bool showBookingForm = false;
    private bool isSubmitting = false;
    private string activeDetailTab = "amenities";
    private string? bookingMessage;
    private bool bookingSuccess = false;
    private string? currentUserId;
    private bool isAuthenticated = false;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            // Resolve authentication state
            var authState = await AuthStateProvider.GetAuthenticationStateAsync();
            isAuthenticated = authState.User.Identity?.IsAuthenticated == true;

            if (isAuthenticated)
            {
                var nameClaim = authState.User.FindFirst(ClaimTypes.Name)?.Value ?? "";
                currentUserId = nameClaim.Split(',').FirstOrDefault();
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error resolving auth state: {ex.Message}");
        }

        try
        {
            hotel = await HotelService.GetHotelWithDetailsAsync(HotelId);
            
            // Visibility check: unapproved hotels are only visible to their owner or admin
            if (hotel != null && !(hotel.IsPublished && hotel.IsApproved))
            {
                // For now, hide unapproved hotels from regular users
                // Owner and admin pages have their own routes
                hotel = null;
            }
        }
        catch (Exception)
        {
            hotel = null;
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task SetActiveTab(string tab)
    {
        try
        {
            activeDetailTab = tab;
            if (tab == "location" && hotel?.Latitude != null && hotel?.Longitude != null)
            {
                Console.WriteLine($"Initializing map for hotel {hotel.Id} at {hotel.Latitude}, {hotel.Longitude}");
                // Give it a small delay to ensure the div is rendered
                await Task.Delay(150);
                await JSRuntime.InvokeVoidAsync("mapHelper.initHotelMap", "hotelMap", (double)hotel.Latitude.Value, (double)hotel.Longitude.Value);
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error in SetActiveTab: {ex.Message}");
        }
    }

    private async Task SubmitBooking()
    {
        if (string.IsNullOrEmpty(currentUserId) || hotel == null) return;

        isSubmitting = true;
        bookingMessage = null;

        try
        {
            var booking = new Booking
            {
                UserId = currentUserId,
                HotelId = hotel.Id,
                StartDate = bookingRequest.StartDate,
                EndDate = bookingRequest.EndDate,
                HeadCount = bookingRequest.HeadCount,
                Notes = bookingRequest.Notes
            };

            await BookingService.CreateBookingAsync(booking);
            
            bookingSuccess = true;
            bookingMessage = "Booking request submitted successfully! The hotel owner will review your request.";
            showBookingForm = false;
        }
        catch (Exception ex)
        {
            bookingSuccess = false;
            bookingMessage = $"Error submitting booking: {ex.Message}";
        }
        finally
        {
            isSubmitting = false;
        }
    }

    private void SendMessage()
    {
        NavigationManager.NavigateTo($"/messages?hotelId={HotelId}");
    }

    private void HandleBookNow()
    {
        if (!isAuthenticated)
        {
            // Encode the current hotel page URL as the return destination
            var returnUrl = Uri.EscapeDataString($"/booking/checkout/{HotelId}");
            NavigationManager.NavigateTo($"/login?returnUrl={returnUrl}", forceLoad: false);
            return;
        }

        NavigationManager.NavigateTo($"/booking/checkout/{HotelId}");
    }

    private void GoToCheckout()
    {
        Console.WriteLine($"GoToCheckout called for HotelId: {HotelId}");
        if (HotelId > 0)
        {
            NavigationManager.NavigateTo($"/booking/checkout/{HotelId}");
        }
        else
        {
            Console.WriteLine("HotelId is invalid");
        }
    }

    // Image gallery state
    private string? selectedImage = null;
    private int selectedImageIndex = 0;

    private void SelectImage(int index, string imageUrl)
    {
        selectedImageIndex = index;
        selectedImage = imageUrl;
    }

    public class BookingRequest
    {
        [Required]
        public DateTime StartDate { get; set; } = DateTime.Today.AddDays(1);
        
        [Required]
        public DateTime EndDate { get; set; } = DateTime.Today.AddDays(2);
        
        [Required]
        [Range(1, 10000)]
        public int HeadCount { get; set; } = 1;
        
        public string? Notes { get; set; }
    }
}
