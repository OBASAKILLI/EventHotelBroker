@page "/"
@rendermode InteractiveServer
@using EventHotelBroker.Services
@using EventHotelBroker.Models
@inject IHotelService HotelService
@inject IEventService EventService
@inject NavigationManager Navigation

<PageTitle>Home - EventHotelBroker</PageTitle>

<!-- Modern Hero Section -->
<div class="modern-hero">
    <div class="hero-background"></div>
    <div class="hero-content">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6 animate-fade-in">
                    <div class="hero-badge">
                        <i class="bi bi-star-fill"></i>
                        <span>Trusted by 10,000+ Event Planners</span>
                    </div>
                    <h1 class="hero-title">
                        Find Your Perfect
                        <span class="gradient-text">Event Venue</span>
                    </h1>
                    <p class="hero-subtitle">
                        Connect with premium hotels, venues, and service providers for your next event.
                        From weddings to conferences, we've got you covered.
                    </p>
                    <div class="hero-search">
                        <div class="search-box">
                            <i class="bi bi-search"></i>
                            <input type="text" placeholder="Search venues, hotels, locations..." />
                            <button class="btn-modern btn-gradient-primary">Search</button>
                        </div>
                    </div>
                    <div class="hero-stats">
                        <div class="stat-item">
                            <div class="stat-number">500+</div>
                            <div class="stat-label">Venues</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-number">50+</div>
                            <div class="stat-label">Cities</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-number">10K+</div>
                            <div class="stat-label">Happy Clients</div>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 d-none d-lg-block">
                    <div class="hero-image">
                        <div class="floating-card card-1">
                            <i class="bi bi-building"></i>
                            <div>
                                <div class="card-title">Premium Hotels</div>
                                <div class="card-subtitle">200+ Venues</div>
                            </div>
                        </div>
                        <div class="floating-card card-2">
                            <i class="bi bi-calendar-check"></i>
                            <div>
                                <div class="card-title">Easy Booking</div>
                                <div class="card-subtitle">Instant Confirmation</div>
                            </div>
                        </div>
                        <div class="floating-card card-3">
                            <i class="bi bi-star-fill"></i>
                            <div>
                                <div class="card-title">Top Rated</div>
                                <div class="card-subtitle">4.9/5 Average</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Quick Access Section -->
<div class="quick-access-section py-5">
    <div class="container">
        <div class="row g-4">
            <div class="col-md-3 col-sm-6">
                <a href="/login" class="quick-access-card">
                    <div class="quick-icon" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                        <i class="bi bi-person-circle"></i>
                    </div>
                    <h6>Login / Register</h6>
                    <p>Access your account</p>
                </a>
            </div>
            <div class="col-md-3 col-sm-6">
                <a href="/user/dashboard" class="quick-access-card">
                    <div class="quick-icon" style="background: linear-gradient(135deg, #10b981 0%, #059669 100%);">
                        <i class="bi bi-speedometer2"></i>
                    </div>
                    <h6>My Dashboard</h6>
                    <p>View your bookings</p>
                </a>
            </div>
            <div class="col-md-3 col-sm-6">
                <a href="/chat" class="quick-access-card">
                    <div class="quick-icon" style="background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);">
                        <i class="bi bi-chat-dots"></i>
                        <span class="quick-badge">3</span>
                    </div>
                    <h6>Messages</h6>
                    <p>Chat with venues</p>
                </a>
            </div>
            <div class="col-md-3 col-sm-6">
                <a href="/owner/analytics" class="quick-access-card">
                    <div class="quick-icon" style="background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);">
                        <i class="bi bi-graph-up"></i>
                    </div>
                    <h6>Analytics</h6>
                    <p>View performance</p>
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .quick-access-section {
        background: #f8f9fa;
    }

    .quick-access-card {
        background: white;
        border-radius: 1rem;
        padding: 2rem 1.5rem;
        text-align: center;
        transition: all 0.3s;
        text-decoration: none;
        color: inherit;
        display: block;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }

    .quick-access-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 24px rgba(0,0,0,0.15);
    }

    .quick-icon {
        width: 70px;
        height: 70px;
        border-radius: 50%;
        margin: 0 auto 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 2rem;
        position: relative;
    }

    .quick-badge {
        position: absolute;
        top: -5px;
        right: -5px;
        background: #ef4444;
        color: white;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 600;
        border: 3px solid white;
    }

    .quick-access-card h6 {
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    .quick-access-card p {
        color: #6c757d;
        margin-bottom: 0;
        font-size: 0.875rem;
    }
</style>

<!-- How It Works Section -->
<div class="how-it-works-section">
    <div class="container">
        <div class="section-header text-center">
            <span class="section-badge">Simple Process</span>
            <h2 class="section-title">How It Works</h2>
            <p class="section-subtitle">Book your perfect venue in three easy steps</p>
        </div>
        <div class="row g-4">
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-number">01</div>
                    <div class="feature-icon">
                        <i class="bi bi-search"></i>
                    </div>
                    <h5 class="feature-title">Search & Discover</h5>
                    <p class="feature-text">Browse through hundreds of verified venues and service providers in your area.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-number">02</div>
                    <div class="feature-icon">
                        <i class="bi bi-chat-dots"></i>
                    </div>
                    <h5 class="feature-title">Connect & Inquire</h5>
                    <p class="feature-text">Message venue owners directly and get instant responses to your questions.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-card">
                    <div class="feature-number">03</div>
                    <div class="feature-icon">
                        <i class="bi bi-calendar-check"></i>
                    </div>
                    <h5 class="feature-title">Book with Confidence</h5>
                    <p class="feature-text">Send booking requests and manage everything from your dashboard.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Explore Hotels & Events Section with Tabs -->
<div class="explore-section py-5">
    <div class="container">
        <div class="section-header text-center mb-4">
            <span class="section-badge">Explore Our Services</span>
            <h2 class="section-title">Hotels & Event Packages</h2>
            <p class="section-subtitle">Everything you need for your perfect event in one place</p>
        </div>

        <!-- Modern Tabs -->
        <div class="modern-tabs-container mb-4">
            <ul class="nav nav-pills modern-tabs justify-content-center" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == "hotels" ? "active" : "")" 
                            @onclick='() => SetActiveTab("hotels")'
                            type="button">
                        <i class="bi bi-building me-2"></i>
                        <span>Hotels & Venues</span>
                        <span class="tab-count">@(featuredHotels?.Count() ?? 0)</span>
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == "events" ? "active" : "")" 
                            @onclick='() => SetActiveTab("events")'
                            type="button">
                        <i class="bi bi-gift me-2"></i>
                        <span>Event Packages</span>
                        <span class="tab-count">@(featuredPackages?.Count() ?? 0)</span>
                    </button>
                </li>
            </ul>
        </div>

        <!-- Tab Content -->
        <div class="tab-content">
            @if (activeTab == "hotels")
            {
                <!-- Hotels Tab -->
                <div class="tab-pane-content animate-fade-in">
                    @if (featuredHotels == null)
                    {
                        <div class="text-center py-5">
                            <div class="spinner-border spinner-modern text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            <p class="mt-3 text-muted">Loading venues...</p>
                        </div>
                    }
                    else if (!featuredHotels.Any())
                    {
                        <div class="modern-card">
                            <div class="empty-state">
                                <i class="bi bi-building empty-state-icon"></i>
                                <h3 class="empty-state-title">No Venues Available</h3>
                                <p class="empty-state-text">Check back soon for amazing venues!</p>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="row g-4">
                            @foreach (var hotel in featuredHotels.Take(6))
                            {
                                <div class="col-md-6 col-lg-4">
                                    <div class="venue-card" @onclick="() => ViewHotelDetails(hotel.Id)">
                                        <div class="venue-image">
                                            @if (hotel.Images?.Any() == true)
                                            {
                                                <img src="@hotel.Images.First().Url" alt="@hotel.Name" />
                                            }
                                            else
                                            {
                                                <div class="venue-placeholder">
                                                    <i class="bi bi-building"></i>
                                                </div>
                                            }
                                            <div class="venue-badge">
                                                <i class="bi bi-star-fill"></i>
                                                <span>Featured</span>
                                            </div>
                                        </div>
                                        <div class="venue-content">
                                            <h5 class="venue-title">@hotel.Name</h5>
                                            <p class="venue-location">
                                                <i class="bi bi-geo-alt"></i>
                                                @hotel.City, @hotel.Country
                                            </p>
                                            <div class="venue-meta">
                                                <span class="meta-item">
                                                    <i class="bi bi-people"></i>
                                                    @hotel.Capacity guests
                                                </span>
                                                <span class="meta-item">
                                                    <i class="bi bi-star-fill text-warning"></i>
                                                    4.8
                                                </span>
                                            </div>
                                            <div class="venue-footer">
                                                <div class="venue-price">
                                                    <span class="price-label">From</span>
                                                    <span class="price-amount">@hotel.Currency @hotel.PricePerNight.ToString("N0")</span>
                                                    <span class="price-period">/night</span>
                                                </div>
                                                <button class="btn btn-sm btn-outline-primary" @onclick:stopPropagation="true">
                                                    View Details
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="text-center mt-4">
                            <a href="/search" class="btn-modern btn-gradient-primary">
                                <i class="bi bi-search me-2"></i>Explore All Hotels
                            </a>
                        </div>
                    }
                </div>
            }
            else
            {
                <!-- Events Tab -->
                <div class="tab-pane-content animate-fade-in">
                    @if (featuredPackages == null)
                    {
                        <div class="text-center py-5">
                            <div class="spinner-border spinner-modern text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            <p class="mt-3 text-muted">Loading event packages...</p>
                        </div>
                    }
                    else if (!featuredPackages.Any())
                    {
                        <div class="modern-card">
                            <div class="empty-state">
                                <i class="bi bi-gift empty-state-icon"></i>
                                <h3 class="empty-state-title">No Event Packages Available</h3>
                                <p class="empty-state-text">Check back soon for amazing event packages!</p>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="row g-4">
                            @foreach (var package in featuredPackages.Take(6))
                            {
                                <div class="col-md-6 col-lg-4">
                                    <div class="package-card-home" @onclick="() => ViewPackageDetails(package.Id)">
                                        <div class="package-image">
                                            @if (!string.IsNullOrEmpty(package.ImageUrl))
                                            {
                                                <img src="@package.ImageUrl" alt="@package.Name" />
                                            }
                                            else
                                            {
                                                <div class="package-placeholder">
                                                    <i class="bi bi-gift"></i>
                                                </div>
                                            }
                                            @if (package.IsFeatured)
                                            {
                                                <div class="package-badge">
                                                    <i class="bi bi-star-fill"></i>
                                                    <span>Featured</span>
                                                </div>
                                            }
                                            @if (package.DiscountedPrice.HasValue)
                                            {
                                                var savings = ((package.TotalPrice - package.DiscountedPrice.Value) / package.TotalPrice) * 100;
                                                <div class="discount-badge">
                                                    @savings.ToString("N0")% OFF
                                                </div>
                                            }
                                        </div>
                                        <div class="package-content">
                                            <span class="package-type-badge">
                                                <i class="bi bi-tag me-1"></i>@package.PackageType
                                            </span>
                                            <h5 class="package-title">@package.Name</h5>
                                            <p class="package-description">
                                                @(package.Description.Length > 80 ? package.Description.Substring(0, 80) + "..." : package.Description)
                                            </p>
                                            <div class="package-meta">
                                                <span class="meta-item">
                                                    <i class="bi bi-people"></i>
                                                    @package.MinGuests - @package.MaxGuests guests
                                                </span>
                                                @if (package.IsCustomizable)
                                                {
                                                    <span class="meta-item">
                                                        <i class="bi bi-sliders text-success"></i>
                                                        Customizable
                                                    </span>
                                                }
                                            </div>
                                            <div class="package-footer">
                                                <div class="package-price">
                                                    @if (package.DiscountedPrice.HasValue)
                                                    {
                                                        <span class="price-original">$@package.TotalPrice.ToString("N0")</span>
                                                        <span class="price-discounted">$@package.DiscountedPrice.Value.ToString("N0")</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="price-amount">$@package.TotalPrice.ToString("N0")</span>
                                                    }
                                                </div>
                                                <button class="btn btn-sm btn-outline-primary" @onclick:stopPropagation="true">
                                                    View Details
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="text-center mt-4">
                            <a href="/events" class="btn-modern btn-gradient-accent">
                                <i class="bi bi-gift me-2"></i>Explore All Event Packages
                            </a>
                        </div>
                    }
                </div>
            }
        </div>
    </div>
</div>

<!-- Featured Venues Section (Legacy - Can be removed if tabs work well) -->
<div class="featured-section d-none">
    <div class="container">
        <div class="section-header text-center">
            <span class="section-badge">Top Picks</span>
            <h2 class="section-title">Featured Venues</h2>
            <p class="section-subtitle">Handpicked venues for your special events</p>
        </div>
        @if (featuredHotels == null)
        {
            <div class="text-center py-5">
                <div class="spinner-border spinner-modern text-primary" role="status">
                    <span class="visually-hidden">Loading...</span>
                </div>
                <p class="mt-3 text-muted">Loading venues...</p>
            </div>
        }
        else if (!featuredHotels.Any())
        {
            <div class="modern-card">
                <div class="empty-state">
                    <i class="bi bi-building empty-state-icon"></i>
                    <h3 class="empty-state-title">No Venues Available</h3>
                    <p class="empty-state-text">Check back soon for amazing venues!</p>
                </div>
            </div>
        }
        else
        {
            <div class="row g-4">
                @foreach (var hotel in featuredHotels.Take(6))
                {
                    <div class="col-md-6 col-lg-4">
                        <div class="venue-card">
                            <div class="venue-image">
                                @if (hotel.Images?.Any() == true)
                                {
                                    <img src="@hotel.Images.First().Url" alt="@hotel.Name" />
                                }
                                else
                                {
                                    <div class="venue-placeholder">
                                        <i class="bi bi-building"></i>
                                    </div>
                                }
                                <div class="venue-badge">
                                    <i class="bi bi-star-fill"></i>
                                    <span>Featured</span>
                                </div>
                            </div>
                            <div class="venue-content">
                                <h5 class="venue-title">@hotel.Name</h5>
                                <p class="venue-location">
                                    <i class="bi bi-geo-alt"></i> @hotel.City, @hotel.Country
                                </p>
                                <div class="venue-details">
                                    <span class="venue-capacity">
                                        <i class="bi bi-people"></i> @hotel.Capacity guests
                                    </span>
                                </div>
                                <div class="venue-footer">
                                    <div class="venue-price">
                                        <span class="price-amount">@hotel.Currency @hotel.PricePerNight.ToString("N0")</span>
                                        <span class="price-period">/night</span>
                                    </div>
                                    <a href="/hotels/@hotel.Id" class="btn-modern btn-gradient-primary btn-sm">
                                        View Details
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div class="text-center mt-5">
                <a href="/search" class="btn-modern btn-gradient-primary" style="padding: 1rem 3rem;">
                    <i class="bi bi-grid me-2"></i>View All Venues
                </a>
            </div>
        }
    </div>
</div>

<!-- CTA Section -->
<div class="cta-section">
    <div class="container">
        <div class="cta-card">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <div class="cta-icon">
                        <i class="bi bi-shop"></i>
                    </div>
                    <h2 class="cta-title">Are You a Venue Owner?</h2>
                    <p class="cta-text">
                        List your hotel, venue, or service on EventHotelBroker and reach thousands of event planners and tourists.
                    </p>
                    <div class="cta-features">
                        <div class="cta-feature">
                            <i class="bi bi-check-circle-fill"></i>
                            <span>Free to list</span>
                        </div>
                        <div class="cta-feature">
                            <i class="bi bi-check-circle-fill"></i>
                            <span>Manage bookings easily</span>
                        </div>
                        <div class="cta-feature">
                            <i class="bi bi-check-circle-fill"></i>
                            <span>Direct communication with clients</span>
                        </div>
                        <div class="cta-feature">
                            <i class="bi bi-check-circle-fill"></i>
                            <span>Analytics and insights</span>
                        </div>
                    </div>
                    <a href="/owner/create-hotel" class="btn-modern btn-gradient-success" style="padding: 1rem 2rem; font-size: 1.1rem;">
                        <i class="bi bi-plus-circle me-2"></i>List Your Venue
                    </a>
                </div>
                <div class="col-lg-6 d-none d-lg-block">
                    <div class="cta-image">
                        <div class="cta-stat">
                            <div class="stat-value">500+</div>
                            <div class="stat-label">Active Venues</div>
                        </div>
                        <div class="cta-stat" style="animation-delay: 0.2s;">
                            <div class="stat-value">10K+</div>
                            <div class="stat-label">Monthly Visitors</div>
                        </div>
                        <div class="cta-stat" style="animation-delay: 0.4s;">
                            <div class="stat-value">95%</div>
                            <div class="stat-label">Satisfaction Rate</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private IEnumerable<Hotel>? featuredHotels;
    private IEnumerable<EventPackage>? featuredPackages;
    private string activeTab = "hotels";

    protected override async Task OnInitializedAsync()
    {
        try
        {
            // Load hotels
            featuredHotels = await HotelService.GetPublishedHotelsAsync();
            
            // Load featured event packages
            var allPackages = await EventService.GetApprovedPackagesAsync();
            featuredPackages = allPackages.Where(p => p.IsFeatured || p.IsActive).Take(6);
        }
        catch (Exception)
        {
            featuredHotels = new List<Hotel>();
            featuredPackages = new List<EventPackage>();
        }
    }

    private void SetActiveTab(string tab)
    {
        activeTab = tab;
    }

    private void ViewHotelDetails(int hotelId)
    {
        Navigation.NavigateTo($"/hotels/{hotelId}");
    }

    private void ViewPackageDetails(int packageId)
    {
        Navigation.NavigateTo($"/events/package/{packageId}");
    }
}
